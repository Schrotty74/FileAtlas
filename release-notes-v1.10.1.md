## Fixed

- Fixed recursive scanning after regular archive files so nested folders and installer packages remain indexed.
- Fixed format filtering to match the final filename extension consistently for bundle and archive entries.
