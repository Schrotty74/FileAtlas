name: CI

# Reiner Build-Check bei jedem Push/PR — erstellt kein Release, keine
# Artefakte. Getrennt von release.yml (das nur manuell per workflow_dispatch
# laeuft), damit sich beide nicht gegenseitig stoeren.
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build and test (Debug, unsigned)
        run: |
          xcodebuild \
            -project FileAtlas.xcodeproj \
            -scheme FileAtlas \
            -configuration Debug \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            test

  manuals:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test manual checker
        run: swift scripts/check-manuals.swift --self-test
      - name: Check both manuals against reviewed baseline
        run: swift scripts/check-manuals.swift
